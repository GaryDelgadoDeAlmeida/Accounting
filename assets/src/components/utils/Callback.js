import React from "react";

/**
 * This component will process the redirect from Auth0 after a user has been authenticated. It will be activated at the http://localhost:8000/callback endpoint.
 */
export default function Callback() {
    // 
}